apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-multi-branch
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes-multi-branch
  template:
    metadata:
      labels:
        app: kubernetes-multi-branch
    spec:
      containers:
      - name: kubernetes-multi-branch
        image: REPLACE_DOCKER_REPO:dev
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-multi-branch
  namespace: dev
spec:
  selector:
    app: kubernetes-multi-branch
  ports:
  - port: 80
    targetPort: 3000
